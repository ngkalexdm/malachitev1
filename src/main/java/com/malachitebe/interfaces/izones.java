package com.malachitebe.interfaces;

import java.util.List;

import com.malachitebe.models.regroupementModel;

public interface izones {
   List<regroupementModel> GetZones();
     /*  RegroupementsModels GetZone(int Idzone);
       bool CreateZone(RegroupementsModels model);
       bool EditZone(RegroupementsModels model);
       bool Save();*/
}
